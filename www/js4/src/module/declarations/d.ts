/**
 * Так как langLabels подгружаются из сгенерированного именованого модуля,
 * то физически он не существует как файл.
 * Чтобы обработать эту ситуацию, создаем декларацию на этот модуль.
 */
declare module "langLabels" {}
